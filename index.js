/**
 * @format
 */

import {AppRegistry} from 'react-native';
import JoliCocktail from './joliCocktail';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => JoliCocktail);
